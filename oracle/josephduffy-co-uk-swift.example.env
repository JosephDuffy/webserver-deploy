# Copy this template to josephduffy-co-uk-swift.env and add any required values.
# Bootstrap never installs this example automatically.
# Installed at /etc/webserver/josephduffy-co-uk-swift.env only when absent.
# The installed file is root-owned and mode 0600.
# This comment-only file is valid while the Swift server needs no environment variables.
